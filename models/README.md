# Model location

Download the trained GP model from Aximo and place it in this folder with exactly this name:

`gp_model_bundle.joblib`

Only use a model bundle that you created or trust. Joblib uses Python pickle internally, so loading an unknown model file can execute malicious code.

